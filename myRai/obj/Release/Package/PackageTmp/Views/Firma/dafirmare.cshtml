@using myRaiHelper;
@using myRaiCommonModel;
@using myRaiCommonManager;
@using MVCProject.Custom_Helpers;

@model TotaliDaFirmareModel

@*<section class="panel" id="tot-car">
    <header class="panel-heading">
        <div class="panel-actions">
            <a href="#" class="panel-action panel-action-toggle" data-panel-toggle=""></a>
        </div>
        <h2 class="panel-title">Da firmare</h2>
    </header>
    <div class="panel-body">



    </div>



</section>*@



        <div class="widget-summary">
            <div class="widget-summary-col widget-summary-col-icon">
                <div class="summary-icon bg-cdf">
                    <i class="icons icon-note"></i>
                </div>
            </div>
            <div class="widget-summary-col">
                <div class="summary">
                    <h3 class="title">Pronti per la firma</h3>
                    <div class="info">
                        <span id="totdoc" class="amount">@Model.Totale</span>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-xs-12" style="margin-top: 25px;">
            @foreach ( var item in Model.RowPerSede )
            {
                if ( item.TotaleSede != 0 )
                {
                    <div class="row font90">
                        <div class="col-sm-8">
                            <b>@item.codiceSede</b> - @item.descSede
                        </div>
                        <div class="col-sm-4 no-pad-left no-pad-right"><span class="text-info">@item.InCarrello docs su </span><span class="@(item.InCarrello == item.TotaleSede ? "text-info" : "")">@item.TotaleSede</span></div>
                    </div>
                }
            }
        </div>
        <div class="col-sm-12">
            <div class="row border-top" style="margin-top: 10px">
                <button @(Model.Totale == 0 ? "disabled='disabled'" : "") id="button-firmadoc" onclick="FirmaDoc()" class="btn bg-cdf" style="margin-top: 10px; width: 100%">FIRMA @Model.Totale.ToString( )  DOCUMENT@(Model.Totale == 1 ? "O" : "I")</button>
            </div>
</div>