@using System.Globalization;
@using myRaiHelper;
@using myRaiHelper;
@using myRaiCommonModel;
@using myRaiCommonModel.raiplace;
@using MVCProject.Custom_Helpers;
@using myRaiCommonManager;

@model CestiniModel

<div class="row no-margin-left-right">
    <div class="params">
        <div class="row">
            @Html.HiddenFor( m => m.richiestaCorrente.flagRisorsa )
            <div class="row no-margin-left-right">
                <div class="params">
                    @Html.LabelFor( m => m.richiestaCorrente.flagRisorsa , "Tipo risorsa" )
                    <div id="">
                        @Html.Label( "Interno" )
                    </div>
                </div>
            </div>
            <div class="row no-margin-left-right">
                <div class="params">
                    @Html.LabelFor( m => m.richiestaCorrente.cognomeRisorsa , "Cognome risorsa" , htmlAttributes: new { id = "labcog" } )
                    <div id="divcogi">
                        @Html.TextBoxFor( m => m.richiestaCorrente.cognomeRisorsa , htmlAttributes: new { @class = "form-control" , required = "required" , @readonly = true } )
                    </div>
                </div>
            </div>
            <div class="row no-margin-left-right" id="divnome">
                <div class="params">
                    @Html.LabelFor( m => m.richiestaCorrente.nomeRisorsa , "Nome risorsa" )
                    @Html.TextBoxFor( m => m.richiestaCorrente.nomeRisorsa , htmlAttributes: new { @class = "form-control" , required = "required" , @readonly = true } )
                </div>
            </div>
            <div id="divmatricola" class="row no-margin-left-right">
                <div class="params">
                    @Html.LabelFor( m => m.richiestaCorrente.matricolaRisorsa , "Matricola risorsa" )
                    @Html.TextBoxFor( m => m.richiestaCorrente.matricolaRisorsa , htmlAttributes: new { @class = "form-control typeahead" , required = "required" , @readonly = true } )
                </div>
            </div>
            <div id="selectorPastoDiv" class="row no-margin-left-right">
                @Html.LabelFor( m => m.richiestaCorrente.tipoCestino )
                <select id="richiestaCorrente_SelectTipoCestino" style="width:100%" class="js-select2-templating form-control val-custom" required="required">
                    @{
                        foreach ( var opt in ( List<myRai.Models.raiplace.Lookup> ) ViewBag.CestiniTypes )
                        {
                            <option value="@(opt.Id)" data-description="@(opt.Description)" @(opt.Selected ? "selected=\"selected\"" : "")>@(opt.Codice)</option>
                        }
                    }
                </select>
                <div class="val-message"></div>
                @Html.HiddenFor( m => m.richiestaCorrente.tipoCestino )
            </div>
        </div>
    </div>
</div>