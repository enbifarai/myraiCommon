@using myRaiHelper;
@using myRaiCommonModel;
@using myRaiCommonManager;
@using MVCProject.Custom_Helpers;

@model NotifichePopupModel

@*<div class="notification-title">

    @if ( Model.tipoNotifiche == 2 )
    {
        @:Notifiche
    }
    else
    {
        @:Cose da fare
    }
</div>

<div class="content">
    <ul>
            @if (Model.Notifiche == null || !Model.Notifiche.Any())
            {
                <li class="text-center">
                    <span class="rai-font-md-neutral">Non ci sono dati da visualizzare</span>
                </li>
            }
            else
            {

                foreach (var item in Model.Notifiche)
                {
            <li>
                @if ( item.id != 0 )
                {
                    <a href="@Url.Action("Index", "Notifiche")?tipo=2" class="clearfix">
                        <span class="title">@item.descrizione</span>
                        <span class="message">@item.data_inserita.ToString( "dd MMMM yyyy" )</span>
                    </a>
                }
                else
                {
                    @Html.Raw( item.descrizione )
                }
            </li>
        }
            }


        </ul>

        <hr />

        <div class="text-left" style="text-align:left;">
            <div class="rai-btn-fill">
                <a href="/notifiche?tipo=@Model.tipoNotifiche" class="btn rai-btn-small">Leggi tutte</a>
            </div>
        </div>
    </div>*@


<div class="rai-popover">
    <div class="rai-popover-header">
        <div class="rai-popover-title">
            @if (Model.tipoNotifiche == 2)
            {
                @:Notifiche
            }
            else
            {
                @:Cose da fare
            }
        </div>
    </div>
    <div class="rai-popover-content">
        <ul>
            @if (Model.Notifiche == null || !Model.Notifiche.Any())
            {
                <li class="text-center">
                    <span class="rai-font-md-neutral">Non ci sono dati da visualizzare</span>
                </li>
            }
            else
            {

                foreach (var item in Model.Notifiche)
                {
                    <li>
                        @if (item.id != 0)
                        {
                            <a href="@Url.Action("Index", "Notifiche")?tipo=2" class="clearfix">
                                <span class="title">@item.descrizione</span>
                                <span class="message">@item.data_inserita.ToString("dd MMMM yyyy")</span>
                            </a>
                        }
                        else
                        {
                            @Html.Raw(item.descrizione)
                        }
                    </li>
                }
            }
                @*	<li>
                        <a href="#" class="clearfix">

                            <span class="title">Rifiutato permesso del 2/05</span>
                            <span class="message">25 aprile 2017</span>
                        </a>
                    </li>
                    <li>
                        <a href="#" class="clearfix">

                            <span class="title">Rifiutato permesso del 2/05</span>
                            <span class="message">25 aprile 2017</span>
                        </a>
                    </li>*@

            </ul>
    </div>
    <div class="rai-popover-footer">
        <div class="rai-btn-fill">
            <a href="/notifiche?tipo=@Model.tipoNotifiche" class="btn rai-btn-small">Leggi tutte</a>
            </div>
        </div>
</div>