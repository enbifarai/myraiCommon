@model myRaiHelper.ModalColumn

<div class="@Model.Width">
    @if (Model.Type == myRaiHelper.ModalColumnType.Main)
    {
        <section class="rai panel">
            <header class="panel-heading">
                <h2 class="panel-title">@Model.Title</h2>
            </header>
            <div class="panel-body">
                @foreach (var section in Model.Sections)
                {
                    switch (section.Type)
                    {
                        case myRaiHelper.SectionType.Panel:
                            @Html.RenderPanel(section)
                            break;
                        case myRaiHelper.SectionType.Block:
                            @Html.RenderBlock(section)
                            break;
                        default:
                            break;
                    }
                }
            </div>
        </section>
    }
    else
    {
        foreach (var section in Model.Sections)
        {
            switch (section.Type)
            {
                case myRaiHelper.SectionType.Panel:
                    @Html.RenderPanel(section)
                    break;
                case myRaiHelper.SectionType.Block:
                    @Html.RenderBlock(section)
                    break;
                default:
                    break;
            }
        }
    }
</div>