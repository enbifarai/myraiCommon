@using myRaiHelper;
@using myRaiCommonModel;
@using myRaiCommonManager;
@using MVCProject.Custom_Helpers;

@model limitaTRmodel

@if ( Model.elementsInList > Model.rows )
{
    <table onload="alert('table')" class="js-table-sections table table-hover" style="width: 100%">
        <tbody class="">
            <tr>
                <td style="width: 50%; text-align: right; padding-right: 15px">
                    <a style="font-weight:bold" class="cuttable taglia-tab pointer" data-tab="@Model.tabella" tabindex="0">MOSTRA DI PIU</a>
                </td>
                <td style="width: 50%; padding-left: 15px">
                    <a style="font-weight:bold" class="cuttable gray-a" data-div="@Model.div" href="#">TORNA SU</a>
                </td>
            </tr>
        </tbody>
    </table>

    <script>limitaTR("@Model.tabella",parseInt('@Model.rows',10));</script>

}