@using myRaiGestionale.Controllers
@using myRaiHelper
@using myRaiCommonModel
@using myRaiCommonManager
@model StudioModel

@{
    var title = "Nuovo titolo di studio";
    if (!String.IsNullOrWhiteSpace(Model.CodTitolo))
    {
        title = "Modifica titolo di studio";
    }

    var tmpModel = Model;
    tmpModel.FormAction = "Save_DatiStudio";
    tmpModel.FormController = "Anagrafica";
    tmpModel.SubmitAction = "Save_DatiStudio(this, '" + Model.Matricola + "')";
}

@*@using (Html.BeginModal(title))
{
    @Html.Partial("subpartial/Form_TitoliStudio", tmpModel)
}*@

@Html.Partial("subpartial/Form_TitoliStudio", tmpModel)
